---
phase: 09-api-layer
plan: 05
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/app/api/schedule/route.ts
  - src/app/api/history/seasons/route.ts
  - src/app/api/history/games/route.ts
  - src/app/api/history/games/[game_id]/route.ts
autonomous: true
requirements:
  - API-04
  - API-05
  - API-07

must_haves:
  truths:
    - "GET /api/schedule returns upcoming LAC games (game_date >= today) with odds:null when no odds snapshot exists"
    - "GET /api/history/seasons returns available season_ids derived from the games table (no hardcoding)"
    - "GET /api/history/games returns paginated LAC game list for a season_id; requires season_id param"
    - "GET /api/history/games/{id} returns box_score with available:false for pre-Phase-7 games (no game_player_box_scores rows)"
    - "GET /api/history/games/{id} returns box_score with available:true when game_player_box_scores rows exist"
    - "GET /api/history/games/{id} returns insights from the insights table WHERE game_id=id; empty array if none"
    - "meta envelope has all 5 required fields on all responses"
  artifacts:
    - path: src/app/api/schedule/route.ts
      provides: "GET /api/schedule handler"
      exports: ["GET"]
    - path: src/app/api/history/seasons/route.ts
      provides: "GET /api/history/seasons handler"
      exports: ["GET"]
    - path: src/app/api/history/games/route.ts
      provides: "GET /api/history/games handler with cursor pagination"
      exports: ["GET"]
    - path: src/app/api/history/games/[game_id]/route.ts
      provides: "GET /api/history/games/{game_id} handler"
      exports: ["GET"]
  key_links:
    - from: src/app/api/history/games/[game_id]/route.ts
      to: src/lib/db.ts
      via: "insights table queried as 'insights' (NOT 'generated_insights')"
---

<objective>
Implement the schedule and history endpoint group: GET /api/schedule, GET /api/history/seasons, GET /api/history/games, and GET /api/history/games/{game_id}.

Purpose: Powers the Schedule page and Historical Explorer. These are read-only queries against stable DB data — the SQL is straightforward but the box_score availability flag and cursor pagination need care.

Output: 4 route files implementing the full schedule + history surface.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-api-layer/09-CONTEXT.md
@.planning/phases/09-api-layer/09-RESEARCH.md
@.planning/phases/09-api-layer/09-01-SUMMARY.md

<interfaces>
<!-- Key contracts. -->

From src/lib/db.ts:
```typescript
export const sql: postgres.Sql;
export const LAC_NBA_TEAM_ID = 1610612746;
```

CRITICAL naming pitfall:
- Insight table name is `insights` (NOT `generated_insights`) — "generated_insights" does not exist in DB

games table: game_id (bigint), nba_game_id (bigint), game_date (date), season_id (int),
  home_team_id (bigint), away_team_id (bigint), status (text),
  home_score (int nullable), away_score (int nullable), start_time_utc (timestamptz nullable)

teams table: team_id (bigint), abbreviation (text), city (text), name (text)

game_player_box_scores: game_id (bigint), player_id (bigint), team_id (bigint)
  (existence of rows for a game_id = available:true)

insights table: insight_id (uuid), scope (text), category (text), headline (text),
  detail (text), importance (int), is_active (boolean), game_id (bigint nullable),
  proof_sql, proof_params (jsonb), proof_result (jsonb), created_at

Cursor pagination pattern (from RESEARCH.md):
```typescript
// Encode: Buffer.from(JSON.stringify({ game_date, game_id })).toString('base64')
// Decode: JSON.parse(Buffer.from(cursor, 'base64').toString('utf8'))
// WHERE clause: (game_date, game_id) < (cursor_date::date, cursor_game_id::bigint) for DESC
```

API_SPEC.md season label format: season_id 2024 → label "2024-25"
API_SPEC.md /history/games game shape: { game_id, game_date, opponent_abbr, home_away, result:"W"|"L", final_score:{team, opp} }
API_SPEC.md /history/games/{id} box_score: { columns:[...], teams:[{team_abbr, players:[], totals:{}}], available:bool }
  columns: ["MIN","PTS","REB","AST","STL","BLK","TO","FG","3PT","FT","+/-"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GET /api/schedule and GET /api/history/seasons</name>
  <files>src/app/api/schedule/route.ts, src/app/api/history/seasons/route.ts</files>
  <behavior>
    schedule route:
    - Resolve LAC internal team_id from teams WHERE nba_team_id = LAC_NBA_TEAM_ID
    - Query games WHERE (home_team_id=LAC OR away_team_id=LAC) AND game_date >= CURRENT_DATE AND status != 'final' ORDER BY game_date ASC LIMIT 20
    - For each game: call getLatestOdds(game_id::text) in parallel; determine opponent_abbr and home_away
    - next_game: first game in list (or null if none)
    - odds: null if getLatestOdds returns null — never fabricate
    - meta: source='mixed', ttl_seconds=1800
    - Cache-Control: public, max-age=1800

    seasons route:
    - Query DISTINCT season_id FROM games ORDER BY season_id ASC
    - Map season_id to label: season_id N → label "${N}-${String(N+1).slice(-2)}" (e.g. 2024 → "2024-25")
    - meta: source='db', ttl_seconds=86400
    - Cache-Control: public, max-age=86400
  </behavior>
  <action>
    Create src/app/api/schedule/route.ts:
    1. Imports: NextResponse, sql, LAC_NBA_TEAM_ID, buildMeta, buildError, getLatestOdds
    2. Parse query params: from (default today), to (default today+30), include_odds (default true)
    3. Resolve LAC internal team_id
    4. Query upcoming games in date range
    5. For each game, resolve opponent team abbreviation (JOIN teams on opponent team_id)
    6. If include_odds, call Promise.all([getLatestOdds(gameId), ...]) for all games
    7. Build next_game from first result, games array from all results
    8. Return { meta, next_game, games } with Cache-Control: public, max-age=1800

    Create src/app/api/history/seasons/route.ts:
    1. Simple query: SELECT DISTINCT season_id FROM games ORDER BY season_id ASC
    2. Map to { season_id, label } pairs using label formula
    3. Return { meta: buildMeta('db', 86400), seasons }
    4. Cache-Control: public, max-age=86400
  </action>
  <verify>
    <automated>cd /Users/luke/CCC && npm test -- src/lib/api-schedule.test.ts src/lib/api-history.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Both route files exist and export GET. odds:null when no snapshot. Seasons label format correct. TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /api/history/games and GET /api/history/games/{game_id}</name>
  <files>src/app/api/history/games/route.ts, src/app/api/history/games/[game_id]/route.ts</files>
  <behavior>
    games list route:
    - Requires season_id param; return 400 with buildError('BAD_REQUEST', ...) if missing
    - Default limit=82, max=200; cursor-based pagination (base64 JSON { game_date, game_id })
    - Query LAC games for season_id with cursor WHERE clause when cursor provided
    - For each game: determine opponent_abbr, home_away, result (W/L from score), final_score {team, opp}
    - next_cursor: base64-encode last row's {game_date, game_id} if row count == limit, else null
    - meta: source='db', ttl_seconds=86400
    - Cache-Control: public, max-age=86400

    game detail route:
    - Fetch game by game_id with teams joined for abbreviations
    - 404 with buildError if game not found
    - Check if game_player_box_scores rows exist for game_id → available:true or available:false
    - If available:true: query player box scores for both teams, build teams array with player rows and totals
    - If available:false: return box_score { columns:[...], teams:[{team_abbr, players:[], totals:{}}, ...], available:false }
    - Query insights WHERE game_id=game_id AND is_active=true ORDER BY importance DESC; empty array if none
    - CRITICAL: query from `insights` table NOT `generated_insights` table
    - Map insights proof: { summary: category, result: proof_result }
    - meta: source='mixed', ttl_seconds=86400
    - Cache-Control: public, max-age=86400
  </behavior>
  <action>
    Create src/app/api/history/games/route.ts:
    1. Parse season_id (required), limit (default 82, max 200), cursor (optional), home_away, result filters
    2. Decode cursor if provided
    3. Build WHERE clause for LAC + season_id + cursor condition + optional filters:
    ```sql
    WHERE (g.home_team_id = ${lacTeamId} OR g.away_team_id = ${lacTeamId})
      AND g.season_id = ${seasonId}
      -- if cursor: AND (g.game_date, g.game_id) < (${cursorDate}::date, ${cursorGameId}::bigint)
      -- if home_away filter: AND (home_away condition)
      -- if result filter: AND (result condition)
    ORDER BY g.game_date DESC, g.game_id DESC
    LIMIT ${limit + 1}  -- fetch one extra to detect if there's a next page
    ```
    4. If rows.length > limit: has_more=true, slice to limit, encode next_cursor from last row
    5. Map each game: home_away from LAC team_id match, result W/L from scores, opponent_abbr from teams join, final_score {team: LAC_score, opp: opp_score}

    Create src/app/api/history/games/[game_id]/route.ts:
    1. Validate game_id param; fetch game with home/away team abbreviations
    2. Check availability: `SELECT COUNT(*)::int AS cnt FROM game_player_box_scores WHERE game_id = ${gameId}::bigint`
    3. If available:
       - Query both teams' players from game_player_box_scores JOIN players
       - Build player rows: { player_id::text, name: display_name, MIN, PTS, REB, AST, STL, BLK, TO, FG, 3PT, FT, +/- }
       - Build totals for each team (sum PTS, REB, AST, TO; format FG/3PT/FT as "made-att")
    4. Query insights: `SELECT ... FROM insights WHERE game_id = ${gameId}::bigint AND is_active = true ORDER BY importance DESC`
    5. Map to response shape
  </action>
  <verify>
    <automated>cd /Users/luke/CCC && npm test -- src/lib/api-history.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both history route files exist and export GET. game_id detail returns available:false for games without box scores. insights queried from 'insights' table. Cursor pagination implemented. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- Files: `ls /Users/luke/CCC/src/app/api/schedule/route.ts /Users/luke/CCC/src/app/api/history/seasons/route.ts /Users/luke/CCC/src/app/api/history/games/route.ts /Users/luke/CCC/src/app/api/history/games/*/route.ts`
- No wrong table name: `grep -r "generated_insights" /Users/luke/CCC/src/app/api/` (must be empty)
- Tests: `cd /Users/luke/CCC && npm test -- src/lib/api-schedule.test.ts src/lib/api-history.test.ts`
- TypeScript: `cd /Users/luke/CCC && npx tsc --noEmit 2>&1 | grep "error" | head -10`
</verification>

<success_criteria>
- GET /api/schedule returns upcoming games with odds:null when unavailable
- GET /api/history/seasons returns season list with correct labels
- GET /api/history/games returns paginated game list with correct W/L/scores
- GET /api/history/games/{id} returns available:false for pre-Phase-7 games
- Insights queried from `insights` table (not `generated_insights`)
- npm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-layer/09-05-SUMMARY.md`
</output>
