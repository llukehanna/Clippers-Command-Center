---
phase: 15-reliability-and-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api-home.test.ts
  - src/lib/api-history.test.ts
autonomous: true
requirements:
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "GET /api/home handler completes in under 300ms (wall-clock, mocked DB)"
    - "GET /api/history/games handler completes in under 400ms (wall-clock, mocked DB)"
    - "Stale src/app/api/live/route.ts is deleted to eliminate confusion"
  artifacts:
    - path: "src/lib/api-home.test.ts"
      provides: "Timing SLA test for /api/home, mocked DB"
      min_lines: 25
    - path: "src/lib/api-history.test.ts"
      provides: "Timing SLA test for /api/history/games, mocked DB"
      min_lines: 25
  key_links:
    - from: "src/lib/api-home.test.ts"
      to: "app/api/home/route.ts"
      via: "import { GET } from '../../app/api/home/route'"
      pattern: "import.*GET.*app/api/home/route"
    - from: "src/lib/api-history.test.ts"
      to: "app/api/history/games/route.ts"
      via: "import { GET } from '../../app/api/history/games/route'"
      pattern: "import.*GET.*app/api/history"
---

<objective>
Create minimal timing SLA tests for `/api/home` (PERF-02, < 300ms) and `/api/history/games` (PERF-03, < 400ms). Delete the stale `src/app/api/live/route.ts` duplicate that the research identified as a confusing leftover.

Purpose: Formally close PERF-02 and PERF-03 with automated assertions. Remove the stale file to prevent future test authors from accidentally importing the wrong route.

Output: Two new minimal test files, stale file deleted, full suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-reliability-and-validation/15-CONTEXT.md
@.planning/phases/15-reliability-and-validation/15-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from codebase for the executor. -->

From app/api/home/route.ts — imports:
```typescript
import { sql, LAC_NBA_TEAM_ID } from '@/src/lib/db';
import { buildMeta, buildError } from '@/src/lib/api-utils';
import { getLatestOdds } from '@/src/lib/odds';
```
The handler calls sql multiple times via Promise.all — mock sql to return [] for all calls (empty arrays produce valid empty response). Mock getLatestOdds to return null.

From app/api/history/games/route.ts — same pattern:
```typescript
import { sql } from '@/src/lib/db';
import { buildMeta } from '@/src/lib/api-utils';
```
The handler runs paginated game list query. Mock sql to return [] → returns empty games array with meta envelope.

Vitest include globs (vitest.config.ts):
```
include: ['scripts/lib/**/*.test.ts', 'src/lib/**/*.test.ts']
```
Files in `src/lib/` are automatically picked up — no config change needed.

Timing test pattern:
```typescript
const start = Date.now();
await GET(request);    // some routes need a Request object
const elapsed = Date.now() - start;
expect(elapsed).toBeLessThan(300);
```

Note: /api/home GET has signature `async function GET(): Promise<NextResponse>` (no request param).
Note: /api/history/games GET may need a Request with URL for pagination params — pass `new Request('http://localhost/api/history/games')` if needed.

Stale file to delete: `src/app/api/live/route.ts`
- It uses relative imports (not @/ aliases)
- It is NOT served by Next.js (app/ at root is authoritative)
- Delete it with `fs.unlinkSync` or shell rm — it should not exist after this plan
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create api-home.test.ts and api-history.test.ts timing SLA tests</name>
  <files>src/lib/api-home.test.ts, src/lib/api-history.test.ts</files>
  <action>
    Create `src/lib/api-home.test.ts`:
    - vi.mock '@/src/lib/db' with sql returning [] for all calls (use vi.fn().mockResolvedValue([]))
    - vi.mock '@/src/lib/odds' with getLatestOdds returning null
    - import GET from '../../app/api/home/route'
    - One describe block: 'GET /api/home'
    - Test 1: 'responds in under 300ms with mocked DB' — Date.now() before/after GET(), assert elapsed < 300, also assert response.status === 200
    - Test 2: 'returns meta envelope' — call GET(), body = await response.json(), expect(body.meta).toHaveProperty('generated_at'), expect(body.meta).toHaveProperty('stale')
    - beforeEach: vi.clearAllMocks()

    Create `src/lib/api-history.test.ts`:
    - vi.mock '@/src/lib/db' with sql returning [] for all calls
    - import GET from '../../app/api/history/games/route'
    - One describe block: 'GET /api/history/games'
    - Test 1: 'responds in under 400ms with mocked DB' — construct `new Request('http://localhost/api/history/games')`, call GET(request), assert elapsed < 400, assert response.status === 200
    - Test 2: 'returns meta envelope' — same pattern, assert meta.generated_at and meta.stale present
    - beforeEach: vi.clearAllMocks()

    Keep both files minimal — 30-40 lines each. No exhaustive behavioral tests; those are PERF-02/03 spot-checks only per CONTEXT.md decision.

    If the /api/home handler uses `currentSeasonId()` or any date-dependent logic, the mocked sql returning [] will safely produce an empty response — no date mocking needed.
  </action>
  <verify>
    <automated>npm test -- src/lib/api-home.test.ts src/lib/api-history.test.ts</automated>
  </verify>
  <done>Both test files pass. Timing assertions < threshold. Meta envelope assertions pass. No test failures.</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale src/app/api/live/route.ts duplicate</name>
  <files>src/app/api/live/route.ts</files>
  <action>
    Delete the file `src/app/api/live/route.ts`. This is the stale copy identified in the research.

    The active route is `app/api/live/route.ts` (at project root, uses @/ aliases). The `src/app/api/live/route.ts` file uses relative imports, is not served by Next.js, and causes confusion about which file tests should import.

    After deletion, run `npm run build -- --no-lint` (or just `npm test`) to confirm no import references the deleted file. If any file imports from `../../../src/app/api/live/route` or similar relative paths, fix those imports to point to `app/api/live/route`.

    Check for references first:
    ```
    grep -r "src/app/api/live" /Users/luke/CCC/src /Users/luke/CCC/app --include="*.ts" --include="*.tsx" 2>/dev/null
    ```
    If no references found, simply delete the file. If references exist, redirect them to `app/api/live/route` and then delete.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>File `src/app/api/live/route.ts` does not exist. `npm test` exits 0. No broken imports.</done>
</task>

</tasks>

<verification>
Full test suite must pass after both tasks:
```
npm test
```
Two new test files contribute passing tests. No regressions anywhere.
</verification>

<success_criteria>
- `src/lib/api-home.test.ts` exists and passes (timing < 300ms)
- `src/lib/api-history.test.ts` exists and passes (timing < 400ms)
- `src/app/api/live/route.ts` does not exist
- `npm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/15-reliability-and-validation/15-02-SUMMARY.md`
</output>
