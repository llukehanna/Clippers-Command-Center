---
phase: 16.1-bug-fixes
plan: 01
subsystem: ui, api
tags: [nav, topnav, floating-pill, odds, schedule, home-api]

# Dependency graph
requires:
  - phase: 10.1-design-system-refinement
    provides: Surface component with variant=nav, NavLinkCapsule component
  - phase: 08-odds-integration
    provides: OddsSnapshot type, getLatestOdds helper
provides:
  - Centered floating pill nav renders consistently on all 5 pages
  - Odds API responses use ScheduleTable-compatible field names (spread, moneyline, over_under)
affects: [11-live-game-dashboard, 12-between-games-dashboard, schedule-page]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TopNav uses Surface variant=nav with mx-auto + max-w-max for centered floating pill"
    - "Odds transformed from OddsSnapshot raw fields to LAC-perspective display shape at API layer"

key-files:
  created: []
  modified:
    - components/nav/TopNav.tsx
    - app/layout.tsx
    - app/live/page.tsx
    - src/app/api/schedule/route.ts
    - src/app/api/home/route.ts

key-decisions:
  - "TopNav now renders on all pages including /live — LiveFloatingNav removed from app/live/page.tsx"
  - "Nav is no longer fixed-positioned; layout.tsx main element has no pt-14 offset"
  - "Odds transformed at API layer: spread/moneyline use LAC perspective (home or away based on isHome/lacIsHome flag)"

patterns-established:
  - "Nav pill pattern: Surface variant=nav + mx-auto + max-w-max — single source of truth in TopNav"
  - "Odds display mapping: spread_home/away + moneyline_home/away + total_points → spread/moneyline/over_under with LAC perspective"

requirements-completed: [BUG-01-NAV, BUG-03-ODDS]

# Metrics
duration: 12min
completed: 2026-03-23
---

# Phase 16.1 Plan 01: Nav Centering and Odds Field Mapping Summary

**Centered floating pill nav unified across all 5 pages; odds API now returns spread/moneyline/over_under mapped from LAC perspective so ScheduleTable betting columns populate correctly**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-23T00:00:00Z
- **Completed:** 2026-03-23T00:12:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Rewrote TopNav to use Surface variant=nav with centered pill (mx-auto + max-w-max), removing fixed positioning and the /live early return guard
- Removed LiveFloatingNav from app/live/page.tsx — TopNav in layout.tsx now handles all pages uniformly
- Removed pt-14 from layout.tsx main and -mt-14 from all live page wrappers
- Added odds display transformation in /api/schedule and /api/home: raw OddsSnapshot fields mapped to spread/moneyline/over_under using LAC home/away perspective

## Task Commits

Each task was committed atomically:

1. **Task 1: Unify nav bar to centered floating pill on all pages** - `1be3bfc` (fix)
2. **Task 2: Fix odds field name mismatch in schedule and home API routes** - `d33fcc3` (fix)

## Files Created/Modified
- `components/nav/TopNav.tsx` - Rewritten to centered floating pill using Surface variant=nav, NavLinkCapsule, and mx-auto centering; no longer returns null on /live
- `app/layout.tsx` - Removed pt-14 from main element (nav no longer fixed)
- `app/live/page.tsx` - Removed LiveFloatingNav function and all -mt-14 compensation classes
- `src/app/api/schedule/route.ts` - Added oddsDisplay transformation mapping OddsSnapshot to display-friendly shape with LAC perspective
- `src/app/api/home/route.ts` - Same odds transformation for upcomingWithOdds mapping

## Decisions Made
- TopNav handles /live routing: eliminates duplicate nav code between TopNav and LiveFloatingNav; single source of truth
- Nav not fixed-positioned: padding offset in layout.tsx is unnecessary and was fighting the floating design
- Odds mapped at API layer, not component layer: keeps ScheduleTable lean and consistent across both home and schedule endpoints

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None - both tasks executed cleanly with build verification passing.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Nav is now consistent across all pages — no duplicate nav code
- Odds columns in ScheduleTable will populate when odds_snapshots has data
- Plans 02 and 03 can proceed independently (different bug fixes)

---
*Phase: 16.1-bug-fixes*
*Completed: 2026-03-23*
