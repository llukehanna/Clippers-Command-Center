---
phase: 16.1-bug-fixes
plan: 04
subsystem: database
tags: [postgres, status-normalization, data-pipeline, bug-fix]

# Dependency graph
requires:
  - phase: 16.1-bug-fixes
    provides: BDL sync scripts, finalize-games pipeline, history API routes
provides:
  - normalizeBDLStatus consistently maps BDL 'Final' to lowercase 'final'
  - verification scripts and test fixtures aligned to lowercase 'final'
affects: [data-pipeline, historical-explorer, player-stats, finalize-games]

# Tech tracking
tech-stack:
  added: []
  patterns: [status-value normalization at ingestion boundary]

key-files:
  created: []
  modified:
    - scripts/lib/upserts.ts
    - scripts/_verify_db.ts
    - scripts/verification.sql
    - src/lib/history-utils.test.ts

key-decisions:
  - "BDL 'Final' mapped to lowercase 'final' at normalizeBDLStatus — matches finalize-games query and all API routes"

patterns-established:
  - "Game status values are lowercase throughout the codebase: 'final', 'scheduled', 'in_progress'"

requirements-completed: [BUG-04-HISTORY, BUG-05-PLAYER-STATS]

# Metrics
duration: 8min
completed: 2026-03-23
---

# Phase 16.1 Plan 04: Status Case Normalization Summary

**normalizeBDLStatus fixed to emit lowercase 'final' — eliminating the case mismatch that caused finalize-games to find 0 games and left all history and player stats empty**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-23T00:00:00Z
- **Completed:** 2026-03-23T00:08:00Z
- **Tasks:** 1 of 2 complete (paused at human-action checkpoint)
- **Files modified:** 4

## Accomplishments
- Fixed root cause of BUG-04 and BUG-05: `normalizeBDLStatus` in `scripts/lib/upserts.ts` now maps BDL `"Final"` to `status: 'final'` (was `'Final'`)
- Updated JSDoc comment to reflect the correct lowercase status value
- Fixed `_verify_db.ts` FILTER and WHERE clauses to query `'final'` (lines 23 and 44)
- Fixed `verification.sql` three occurrences to use `'final'` (lines 21, 34, 41)
- Updated `history-utils.test.ts` three test fixtures to use `'final'` instead of `'Final'`

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix normalizeBDLStatus case + verification scripts** - `ee45a7b` (fix)

## Files Created/Modified
- `scripts/lib/upserts.ts` - normalizeBDLStatus now returns `{ status: 'final' }`, JSDoc updated
- `scripts/_verify_db.ts` - FILTER(WHERE status='final') and WHERE status!='final' corrected
- `scripts/verification.sql` - Three 'Final' references updated to 'final'
- `src/lib/history-utils.test.ts` - Three status fixture values updated from 'Final' to 'final'

## Decisions Made
- `raw === 'Final'` input check in normalizeBDLStatus is intentionally left as-is (capital F) — it compares against BDL's raw API response, not a status value being written
- `detectOT('Final')` in history-utils.test.ts is intentionally left as-is — it tests NBA CDN format detection, not the status field

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Pre-existing TypeScript errors in `src/lib/api-live.test.ts` (mock type incompatibilities with ResultQueryMeta). These are unrelated to this plan's changes and were present before execution.

## User Setup Required

**Task 2 requires manual database commands.** The user must run:

1. `UPDATE games SET status = 'final' WHERE status = 'Final';` against the production Neon DB
2. `npm run sync:schedule -- --full-season`
3. `npm run finalize-games -- --since=2025-10-01`
4. `npm run compute-stats`
5. SQL: `UPDATE players SET is_active = true WHERE EXISTS (SELECT 1 FROM player_team_stints pts JOIN teams t ON pts.team_id = t.team_id WHERE pts.player_id = players.player_id AND t.nba_team_id = 13);`

## Next Phase Readiness
- Code fix committed and ready — all status references consistent with lowercase 'final'
- Awaiting human pipeline execution to populate historical games, box scores, and rolling stats
- Once pipeline completes, /history and /players pages should show full 2025-26 season data

---
*Phase: 16.1-bug-fixes*
*Completed: 2026-03-23*
