---
phase: 16.1-bug-fixes
verified: 2026-03-23T20:00:00Z
status: human_needed
score: 5/5 must-haves verified at code level
re_verification:
  previous_status: gaps_found
  previous_score: 3/5
  gaps_closed:
    - "normalizeBDLStatus maps BDL 'Final' to lowercase 'final' — status case mismatch root cause eliminated"
    - "All verification scripts and test fixtures now use lowercase 'final'"
    - "_verify_db.ts and verification.sql updated to query lowercase 'final'"
    - "history-utils.test.ts fixtures updated to use 'final'"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Confirm games table has 50+ rows with status='final' after pipeline re-run"
    expected: "SELECT COUNT(*), status FROM games WHERE season_id=2025 GROUP BY status — shows 50+ rows with status='final'"
    why_human: "Requires DATABASE_URL credentials to query Neon DB. SUMMARY says human completed Task 2 pipeline, but result cannot be confirmed programmatically."
  - test: "Confirm player box scores and rolling stats populated after pipeline re-run"
    expected: "SELECT COUNT(*) FROM game_player_box_scores returns >0; SELECT COUNT(*) FROM rolling_player_stats WHERE season_id=2025 returns >0"
    why_human: "Same credential requirement as above."
  - test: "Inspect /history page for full 2025-26 season data"
    expected: "60+ LAC games with scores and W/L results from October 2025 onward, not just March 2026"
    why_human: "Requires browser access to live app against production DB."
  - test: "Inspect /players page for populated game logs and rolling averages"
    expected: "Clicking any active player shows non-empty game log and numerical rolling averages"
    why_human: "Requires browser access to live app against production DB."
  - test: "Verify betting columns show values when odds data is present"
    expected: "Spread, ML, O/U columns show values (not dashes) for upcoming games with odds_snapshots rows"
    why_human: "No odds data currently in DB for upcoming March 2026 games. Code correct; need odds data to confirm display."
---

# Phase 16.1: Bug Fixes Verification Report

**Phase Goal:** Fix critical bugs preventing MVP data pipeline, history page, and player stats from functioning correctly
**Verified:** 2026-03-23T20:00:00Z
**Status:** human_needed (all code-level gaps closed; DB state requires human confirmation)
**Re-verification:** Yes — after gap closure via Plan 04 (status case normalization, commit 6b35dd6)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Nav bar is centered consistently on all pages | VERIFIED | `TopNav.tsx` line 13: `Surface variant="nav"` with `mx-auto max-w-max`. No `fixed top-0`. No `LiveFloatingNav` in live page. No `pt-14` in layout. |
| 2 | Live game DATA_DELAYED state shows box score and key metrics | VERIFIED | `app/api/live/route.ts` lines 329-354: `computeKeyMetrics(lacBoxStale, oppBoxStale)` and `buildBoxScore(...)` wired; `key_metrics: []` / `box_score: null` confined to NO_ACTIVE_GAME branch only (line 283-284). |
| 3 | Betting columns (spread, ML, O/U) are populated in schedule view | VERIFIED* | `src/app/api/schedule/route.ts` lines 99-101 and `src/app/api/home/route.ts` lines 339-341 map odds fields correctly with LAC perspective. *Visible only when `odds_snapshots` has data — no odds currently in DB. |
| 4 | Historical explorer shows full 2025-26 season data | VERIFIED (code) / NEEDS HUMAN (data) | Root cause fixed: `normalizeBDLStatus` line 128 now writes `{ status: 'final' }`. `finalize-games.ts` queries `WHERE g.status = 'final'` — now matches. Human completed pipeline re-run (Task 2 of Plan 04). DB state requires human confirmation. |
| 5 | Player stats pages show game logs and rolling averages | VERIFIED (code) / NEEDS HUMAN (data) | Same root cause as #4. API route `src/app/api/players/[player_id]/route.ts` is correct. `game_player_box_scores` should have rows after pipeline re-run. DB state requires human confirmation. |

**Score:** 5/5 truths verified at code level. 2 truths (BUG-04, BUG-05) require human DB confirmation.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `components/nav/TopNav.tsx` | Centered floating pill with `mx-auto` + `max-w-max` | VERIFIED | Line 13: `Surface variant="nav" className="relative mx-auto flex max-w-max ..."` |
| `src/app/api/schedule/route.ts` | Odds field mapping with `spread:` | VERIFIED | Lines 99-101: `spread: isHome ? odds.spread_home : odds.spread_away` |
| `src/app/api/home/route.ts` | Odds field mapping with `spread:` | VERIFIED | Lines 339-341: `spread: lacIsHome ? odds.spread_home : odds.spread_away` |
| `app/api/live/route.ts` | DATA_DELAYED branch builds `box_score` and `key_metrics` | VERIFIED | Lines 329-354: `computeKeyMetrics` and `buildBoxScore` wired into DATA_DELAYED response |
| `scripts/lib/upserts.ts` | `normalizeBDLStatus` maps BDL `'Final'` to `'final'` | VERIFIED (FIXED) | Line 128: `if (raw === 'Final') return { status: 'final', startTimeUtc: null };` — was `'Final'`, now `'final'` |
| `scripts/_verify_db.ts` | Queries use lowercase `'final'` | VERIFIED (FIXED) | Lines 23, 44: FILTER and WHERE clauses updated |
| `scripts/verification.sql` | Queries use lowercase `'final'` | VERIFIED (FIXED) | Lines 21, 34, 41: all three references updated |
| `src/lib/history-utils.test.ts` | Test fixtures use lowercase `'final'` | VERIFIED (FIXED) | Lines 22, 31, 49: `status: 'final'` in all three game fixtures |
| `scripts/finalize-games.ts` | Queries `WHERE status = 'final'` (unchanged) | VERIFIED | Lines 32, 41: unchanged — was already correct |
| `scripts/lib/finalize.ts` | Writes `status = 'final'` after finalization (unchanged) | VERIFIED | Line 96: `UPDATE games SET status = 'final'` — unchanged, already correct |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `components/nav/TopNav.tsx` | `components/ui/surface.tsx` | `Surface variant="nav"` | VERIFIED | Line 13: `<Surface variant="nav"` |
| `src/app/api/schedule/route.ts` | `components/home/ScheduleTable.tsx` | `spread` / `moneyline` / `over_under` | VERIFIED | API emits these field names; ScheduleTable reads `g.odds?.spread` etc. — match |
| `app/api/live/route.ts` | `components/live/KeyMetricsRow.tsx` | `key_metrics` array in DATA_DELAYED | VERIFIED | `key_metrics: staleKeyMetrics` at line 353 using `computeKeyMetrics` result |
| `app/api/live/route.ts` | `components/live/BoxScoreModule.tsx` | `box_score` in DATA_DELAYED | VERIFIED | `box_score: staleBoxScore` at line 354 using `buildBoxScore` result |
| `scripts/lib/upserts.ts` | `scripts/finalize-games.ts` | `status` column — both now use lowercase `'final'` | VERIFIED (FIXED) | `upsertGames` writes `'final'`; `finalize-games` queries `WHERE status = 'final'` — now match |
| `scripts/finalize-games.ts` | `game_team_box_scores` / `game_player_box_scores` | `finalizeGame` writes box scores | VERIFIED (code) / NEEDS HUMAN (data) | `finalizeGame` called for each row returned. Will execute if pipeline re-run found completed games. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `TopNav.tsx` | `pathname` | `usePathname()` Next.js hook | Yes — runtime path | FLOWING |
| `app/api/live/route.ts` DATA_DELAYED | `staleKeyMetrics` / `staleBoxScore` | Stale snapshot `home_box` / `away_box` payload | Yes — when snapshot has box data | FLOWING (conditional on stale snapshot) |
| `src/app/api/schedule/route.ts` | `oddsDisplay` | `getLatestOdds(game_id)` from `odds_snapshots` | DB has no odds for upcoming games | STATIC (code correct, no odds data in DB) |
| `scripts/lib/upserts.ts` | `status` written to `games` table | BDL API response `"Final"` | Now writes lowercase `'final'` | FIXED |
| `src/app/api/players/[player_id]/route.ts` | `boxScoreRows` | `game_player_box_scores` | Should have rows after pipeline re-run | NEEDS HUMAN CONFIRMATION |

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| Nav has `mx-auto` centering | `grep "mx-auto" components/nav/TopNav.tsx` | Line 13: present | PASS |
| No `fixed top-0` in nav | `grep "fixed top-0" components/nav/TopNav.tsx` | No match | PASS |
| DATA_DELAYED calls `computeKeyMetrics` | `grep -n "computeKeyMetrics" app/api/live/route.ts` | Lines 119, 329, 386 | PASS |
| `DATA_DELAYED key_metrics: []` removed | Only line 283 (NO_ACTIVE_GAME) has `key_metrics: []` | Confirmed | PASS |
| Schedule API maps `spread:` field | `grep "spread:" src/app/api/schedule/route.ts` | Line 99 | PASS |
| `normalizeBDLStatus` writes `'final'` | `grep "status: 'final'" scripts/lib/upserts.ts` | Line 128 | PASS |
| No `'Final'` writes in active pipeline | `grep -rn "status.*'Final'" scripts/` excl. `_query_games.ts` | No matches | PASS |
| `_verify_db.ts` queries lowercase | `grep "'final'" scripts/_verify_db.ts` | Lines 23, 44 | PASS |
| `verification.sql` uses lowercase | `grep "'final'" scripts/verification.sql` | Lines 21, 34, 41 | PASS |
| `history-utils.test.ts` fixtures fixed | `grep "status: 'final'" src/lib/history-utils.test.ts` | Lines 22, 31, 49 | PASS |
| TypeScript compiles (pre-existing errors only) | `npx tsc --noEmit` | Errors only in `api-live.test.ts` (mock type issues, pre-existing and documented in Plan 04 SUMMARY) | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BUG-01-NAV | 16.1-01-PLAN.md | Nav bar centered floating pill on all pages | SATISFIED | `TopNav.tsx` rewritten with `mx-auto max-w-max`; `layout.tsx` `pt-14` removed; live page `LiveFloatingNav` removed |
| BUG-02-LIVE-STATS | 16.1-02-PLAN.md | DATA_DELAYED returns box score and key metrics | SATISFIED | `app/api/live/route.ts` DATA_DELAYED branch builds stale box score and key metrics |
| BUG-03-ODDS | 16.1-01-PLAN.md | Odds field names match ScheduleTable (`spread`, `moneyline`, `over_under`) | SATISFIED | Both API routes map `OddsSnapshot` fields to display-friendly shape with LAC perspective |
| BUG-04-HISTORY | 16.1-03-PLAN.md + 16.1-04-PLAN.md | Historical explorer shows full 2025-26 season | SATISFIED (code) / NEEDS HUMAN (data) | Root cause fixed in commit `6b35dd6`. `normalizeBDLStatus` now writes `'final'`. Human re-ran pipeline. DB state requires confirmation. |
| BUG-05-PLAYER-STATS | 16.1-03-PLAN.md + 16.1-04-PLAN.md | Player stats show game logs and rolling averages | SATISFIED (code) / NEEDS HUMAN (data) | Same root cause as BUG-04. API route correct. Pipeline re-run by human. DB state requires confirmation. |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/_query_games.ts` | 25 | `AND g.status = 'Final'` | Info | Temporary seed script from Jan 2024 period — not imported or referenced anywhere in the active codebase. Not part of the data pipeline. No action needed. |

No stubs or placeholder implementations found. The one remaining `'Final'` reference is in a dead one-off query script and is not part of any active data flow.

---

### Human Verification Required

#### 1. Confirm games table has 50+ rows with status='final'

**Test:** Connect to production Neon DB and run:
```sql
SELECT COUNT(*), status FROM games WHERE season_id = 2025 GROUP BY status ORDER BY status;
```
**Expected:** 50+ rows with `status='final'` and some rows with `status='scheduled'` for upcoming games
**Why human:** Requires DATABASE_URL credentials. SUMMARY says human completed Task 2 pipeline (SQL fix + sync-schedule + finalize-games + compute-stats), but DB state cannot be confirmed programmatically.

#### 2. Confirm player box scores and rolling stats populated

**Test:** Run:
```sql
SELECT COUNT(*) FROM game_player_box_scores;
SELECT COUNT(*) FROM rolling_player_stats WHERE season_id = 2025;
```
**Expected:** Both return >0
**Why human:** Same credential requirement as #1.

#### 3. Inspect /history page for full season data

**Test:** Visit /history in a browser
**Expected:** 60+ LAC games with scores and W/L results from October 2025 onward — not just from March 2026
**Why human:** Requires browser access to live app against production DB.

#### 4. Inspect /players page for populated stats

**Test:** Visit /players, click any active player
**Expected:** Game log shows individual game rows with stats; rolling averages show numerical values, not dashes
**Why human:** Requires browser access to live app against production DB.

#### 5. Confirm betting columns visible when odds data is present

**Test:** Either insert a test odds row into `odds_snapshots` for an upcoming game and visit /schedule, or wait for odds to sync
**Expected:** Spread, ML, O/U columns show values instead of dashes
**Why human:** No odds data currently in DB for upcoming March 2026 games. Code is correct; need data to confirm display.

---

### Gaps Summary

No code-level gaps remain. All five requirements are implemented correctly.

**BUG-04 and BUG-05 root cause identified and fixed:** `normalizeBDLStatus` in `scripts/lib/upserts.ts` was writing `status: 'Final'` (capital F) while `finalize-games.ts` and all API routes query lowercase `'final'`. This caused `finalize-games` to find zero games to process, leaving `game_player_box_scores` and `rolling_player_stats` empty. Fix applied in commit `6b35dd6` — Plan 04. All secondary files (`_verify_db.ts`, `verification.sql`, `history-utils.test.ts`) updated to be consistent.

**Previous verification status was `gaps_found` (3/5).** This re-verification closes both code gaps. Status is now `human_needed` because the data-level outcome (DB populated after pipeline re-run) requires human confirmation with production DB credentials.

**BUG-01-NAV, BUG-02-LIVE-STATS, BUG-03-ODDS remain verified** — no regressions found.

---

_Verified: 2026-03-23T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
